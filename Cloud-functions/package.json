{
  "name": "can-yoldasi-ahi-functions",
  "version": "1.0.0",
  "description": "Can Yoldaşı AHI için Uzman Beyincikler",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc"
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "private": true
}